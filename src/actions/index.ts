'use server'
import { db } from '@/db';

export async function editSnippet(id: number, code: string) {
    //wire up to client
    console.log(id, code);

    //code to update snippet

    //code to redirect user
}